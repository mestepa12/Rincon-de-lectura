# Modo emulador. Se carga SOLO con `vite --mode emulador`, que es lo que
# hace `npm run dev:emu`. Ni `npm run dev` ni `npm run build` lo leen.
# No contiene credenciales: la config real sigue viniendo de .env.
VITE_USE_EMULATORS=true
