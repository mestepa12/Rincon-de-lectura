.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log
# Carpeta suelta que deja un `emulators:export` interrumpido
firebase-export-*/
.DS_Store
# Dependencias de Node
node_modules/

# Variables de entorno (credenciales)
.env
.env.dev
.env.local
.env.*.local

# Build de Vite
dist/

# Archivos autogenerados por Playwright
playwright-report/
test-results/
# Datos de los emuladores de Firebase (import/export local)
.emuladores/
.emuladores-nuevo/
.emuladores-anterior/

# Recibos de borrado de cuenta (scripts/borrar-cuenta.mjs): llevan el uid
# de una persona real, y el registro lo guardas tú, no el repositorio.
recibos-borrado/
