{
  "scripts": {
    "dev": "vite",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:emu": "node scripts/test-emuladores.mjs",
    "dev:emu": "node scripts/dev-emuladores.mjs",
    "emu:semilla": "node scripts/seed-emuladores.mjs",
    "emu:exportar": "node scripts/exportar-emuladores.mjs",
    "emu:limpiar": "node scripts/limpiar-emuladores.mjs",
    "build": "vite build",
    "build:dev": "vite build --mode dev",
    "preview": "vite preview",
    "deploy:preview": "node scripts/deploy-preview.mjs",
    "deploy:dev": "node scripts/deploy-dev.mjs",
    "reglas:dev": "node scripts/deploy-dev.mjs --only firestore:rules",
    "indices:dev": "node scripts/deploy-dev.mjs --only firestore:indexes",
    "deploy": "node scripts/deploy-prod.mjs",
    "proyecto": "node scripts/proyecto-actual.mjs",
    "canales": "firebase hosting:channel:list --project prod",
    "canal:borrar": "firebase hosting:channel:delete --project prod",
    "android": "npm run build && npx cap sync android && npx cap open android"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@playwright/test": "^1.59.1",
    "firebase": "^12.13.0",
    "vite": "^8.0.11"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.3",
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3",
    "chart.js": "^4.5.1",
    "html2canvas": "^1.4.1"
  }
}
