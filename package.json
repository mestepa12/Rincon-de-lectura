{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "firebase": "^12.13.0",
    "vite": "^8.0.11"
  }
}
